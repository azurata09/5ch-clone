import { Topic } from "../types";
import { Link } from 'react-router-dom'

type Props = {
  topic: Topic
}

const OneTopic: React.FC = (props: Props) => {
  return (
    <></>
      // <Link to={`/topic/${props.topic.id}`}></Link>
  )
}